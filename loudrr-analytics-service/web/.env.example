# Set this in the frontend deployment to go LIVE against the scoring API.
# Leave unset for local dev (uses seeded mock data with the real calibrated scores).
# Must include the /v1 path prefix.
NEXT_PUBLIC_LOUDRR_API=https://api.loudrr.com/v1

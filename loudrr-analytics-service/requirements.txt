# Web / API
fastapi==0.115.6
uvicorn[standard]==0.34.0     # not imported — it's the container CMD
pydantic==2.10.4
pydantic-settings==2.7.1

# HTTP client (the X gateway + coingecko seeding)
httpx==0.28.1
# Async leaky-bucket rate limiter for the paid-API crawl (MIT) — researched pick
aiolimiter==1.2.1

# Storage
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0          # Postgres driver (graph/serving at scale)
aiosqlite==0.20.0        # SQLite driver — used by the test suite's temp DB

# Scoring engine
networkx==3.4.2

# Tooling
python-dotenv==1.0.1
tenacity==9.0.0

# Dev / test
pytest==8.3.4
pytest-asyncio==0.25.0

# REMOVED 2026-08 with the non-scoring subsystems (git history has the pins):
#   numpy / scipy / scikit-learn  — vendor-parity calibration fitting
#   camoufox / playwright / curl_cffi — Kaito mindshare browser scrape (camoufox
#     alone pulled a full Firefox into the image)
#   redis                         — engagement/live fan-out
#   alembic                       — never used here; schema comes from
#                                   Base.metadata.create_all via scripts/init_db.py
